---
openapi: 3.0.1
info:
  title: API V1
  version: v1
paths:
  "/service_orders":
    get:
      summary: returns all service orders
      tags:
      - Service Orders
      responses:
        '200':
          description: success
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  properties:
                    id:
                      type: integer
                    customer_id:
                      type: integer
                    vehicle_id:
                      type: integer
                    opening_date:
                      type: string
                      format: date-time
                    closing_date:
                      type: string
                      format: date-time
                      nullable: true
                    status:
                      type: string
                    description:
                      type: string
                      nullable: true
                    created_at:
                      type: string
                      format: date-time
                    updated_at:
                      type: string
                      format: date-time
                  required:
                  - id
                  - customer_id
                  - vehicle_id
                  - opening_date
                  - status
                  - created_at
                  - updated_at
servers:
- url: http://localhost:3000
