#!/usr/bin/env ruby

ENV["RAILS_ENV"] ||= "development"

require_relative "../config/environment"

Rails.application.eager_load!

require "sneakers"
require "sneakers/cli"

Sneakers::CLI.start(["work", "Infra::Workers::OrderApprovedWorker"])
